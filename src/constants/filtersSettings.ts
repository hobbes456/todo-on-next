interface FilterProps {
    active: string;
    all: string;
    completed: string;
}

export const filtersSettings: FilterProps = {
    active: "Active",
    all: "All",
    completed: "Completed",
};
